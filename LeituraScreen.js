import { View, Text } from "react-native";

export default function LeituraScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 32 }}>Tela de Leitura</Text>
    </View>
  );
}
