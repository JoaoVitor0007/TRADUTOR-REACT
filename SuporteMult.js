import { View, Text } from "react-native";
import SuporteMultScreen from './SuporteMult';

export default function SuporteMultScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 32 }}>Suporte multilingue</Text>
    </View>
  );
}
